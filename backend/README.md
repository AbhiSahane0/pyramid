# Pyramid — Backend (NestJS)

REST API for the Pyramid task management app: auth (Google OAuth + guest, JWT cookies with refresh rotation), workspace-scoped tasks/projects/labels/comments, Prisma + PostgreSQL, Swagger at `/api/docs`.

See the [root README](../README.md) for setup, environment variables, architecture and deployment.
